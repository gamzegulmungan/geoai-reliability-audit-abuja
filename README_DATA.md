`AI_Study_Area.*` (raw Mapflow polygons) is intentionally not redistributed — see main README §Data & licensing.
To reproduce the full pipeline, regenerate the layer via Mapflow.ai ("Buildings" model) over `Study_Area.shp`
and place the shapefile set here, or contact the author.
